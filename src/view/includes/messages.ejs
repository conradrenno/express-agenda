<% if (errors.length> 0) { %>
    <div class="row">
        <div class="col my-3">
            <% errors.forEach(function(error) { %>
                <div class="alert alert-danger">
                    <%= error %><br>
                </div>
                <% }); %>
        </div>
    </div>
    <% } %>